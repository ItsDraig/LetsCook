import React from 'react';
import { Text, View, SafeAreaView, ActivityIndicator, ScrollView, RefreshControl } from 'react-native';

const RecipeDetails = () => {
    return (
        <Text>RecipeDetails</Text>
    )
}

export default RecipeDetails